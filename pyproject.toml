[project]
name = "era5-to-cesm"
version = "1.0.0"
description = "Download ERA5 reanalysis data and convert to CESM atmospheric forcing format"
requires-python = ">=3.10"
dependencies = [
    "cdsapi",
    "netcdf4",
    "numpy",
    "pandas",
    "xarray",
]
